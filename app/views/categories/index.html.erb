<h1>Categories</h1>
  <%= link_to "Add new category", new_category_path, class: "btn btn-primary" %>

<ul>
  <% @categories.each do |category| %>
    <li>
      <%= category.name %>
    </li>
  <% end %>
</ul>
